#include "kappalib.h"


const int N = 100;
int a[100];
int main() {
for (int i = 0; i < N - 1; i++) {
a[i] = i;
}
double* half = (double*)malloc((100)*sizeof(double));
for (int a_i = 0; a_i < (100); ++a_i) {
x = a[a_i];
half[a_i] = x / 2;
}
int* b = (int*)malloc((100)*sizeof(int));
for (int j = 0; j < (100); ++j) {
b[j] = j;
}
}

Accepted!
